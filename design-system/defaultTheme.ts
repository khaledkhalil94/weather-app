import {ThemeOptions} from '@mui/material/styles/createTheme';

/**
 * Default customizations for the main MUI theme
 */

export const defaultTheme: ThemeOptions = {
  palette: {
    primary: {
      main: '#3f51b5',
    },
    secondary: {
      main: '#f50057',
    },
  },
};

export default defaultTheme;
